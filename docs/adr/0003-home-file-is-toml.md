# 一户的描述文件用 TOML

`home.toml` 而不是 YAML 或 JSON：python3 标准库自带 `tomllib`（3.11+），
而 PyYAML 要装、JSON 不能写注释。

零依赖对这件事不是洁癖：生成器要在别人家的现场跑，环境里缺一个 pip 包就卡在那儿。
注释也不是可有可无 —— 这份文件里全是 `blt.3.1ptb0rqc10001` 这种 did，
旁边不能写「沙发人体存在」的话，半年后没人分得清哪个 did 是哪盏灯。
JSON 同时输在这两点上（还不允许尾逗号）。
