# 米家极客版住户自动化

给别人家配置米家自动化和模式看板。规则图存在版本控制里、能 diff、能对账推回中枢；
看板不读抄来的配置，直接从规则图现推。

## Language

### 住户与设备

**户**：
一个米家家庭 + 一台中枢网关 + 一套极客版规则图，是配置与部署的最小单位。
_Avoid_: 家庭、房子、house（代码里用 house）

**区**：
一个人体传感器覆盖、且灯一起亮灭的空间，是「有人开灯 / 无人关灯」这对规则的作用范围。
_Avoid_: 房间（住户口语可以说房间，但一个物理房间可能是两个区，如 1302 的主卧与卧室书桌）

**区 key**：
区的 ASCII 短名，人在配置里给一次，机器拿它拼所有变量名（`xuanGuan` → `luxXuanGuan`）。
_Avoid_: id、slug

**探针**：
定时把设备读数写进变量的规则，是照度等传感器数据进入自动化的唯一途径。
一户一条，按传感器去重 —— 共用传感器的两个区读同一个照度变量。
_Avoid_: 采集器、轮询规则

### 规则的结构

**原型**：
一类规则，参数化之后可以给任意一户生成（如「有人开灯」「手动锁」「意图/执行」）。
_Avoid_: 模板（这里没有整张图的模板，见**形状**）、archetype

**形状**：
某种节点从真实规则图里抠出来的一个样本，生成器照着它填值，不自己写节点。
_Avoid_: 模板、schema

**目标**：
一条规则要动的设备端点，写成 `did` 或 `did:siid`（一个墙壁开关的某个键也是一个目标）。
_Avoid_: 灯、设备（一个目标可能是灯组、单灯，或开关的一个键）

**初值**：
`home.yaml` 里给阈值和延时写的数，只在这条规则**第一次**生成时用；
之后以规则图里的当前值为准（现场和看板都会改它）。
_Avoid_: 默认值、配置值

**生成清单**：
`data/<户>/generated.json`，记着生成器上次产出了哪些规则 id。
不在清单里的规则是手建的，生成器永远不碰。
_Avoid_: manifest、lock 文件

**闸门**：
开灯链上一道「某变量 == 某值」的判断，不成立就不往下走。
_Avoid_: 条件、判断、前置

**手动锁**：
记录住户动过墙壁开关的变量，置位后该区的感应开灯不再执行。
_Avoid_: 手动模式、override

**翻转变量**：
只用来触发一次执行的变量，规则第一步就把它写回 0。
_Avoid_: 同步变量、脉冲（脉冲指服务端发出的那个动作）

**意图 / 执行**：
一个场景拆成的两条规则：意图只改模式变量，执行监听该变量的变化再动设备。
_Avoid_: 触发/动作

### 看板

**角色**：
一个变量在规则图里的用法所决定的身份（模式开关 / 参数 / 只读快照 / 翻转变量），
看板据此决定这张卡能不能改、上下界是多少。
_Avoid_: 类型、kind

**覆盖层**：
配置文件里人写、机器猜不出来的那部分（显示名、平面图位置、话术、藏哪几条规则）。
_Avoid_: 配置、语义地图

## Relationships

- 一**户**有一到多个**区**；每个**区**至少产出「有人开灯」「无人关灯」两条规则
- 一个**区**的开灯规则由若干**闸门**串成一条链，最后一道是照度判断
- **手动锁**是**区**级的，**翻转变量**和**探针**是**户**级的
- 一个**变量**在规则图里的用法决定它的**角色**；看板不读配置，只读**角色**
- **覆盖层**只能改显示，改不了**角色**和闸门链
- 一个**区**有一组**目标**（灯）和一组开关键，后者决定**手动锁**挂几个触发
- **生成清单**里的规则由生成器拥有；**原型**决定它长什么样，**形状**决定每个节点怎么写

## Example dialogue

> **配置的人：** 王家要几个区？
> **勘查的人：** 客厅那个传感器覆盖沙发和餐桌，但灯是两组，分别控。
> **配置的人：** 那就是两个**区**共用一个传感器 —— 区是按「灯一起亮灭」划的，不是按传感器。
> 两个区各给一个 **key**，`shaFa` 和 `canTing`，照度变量会拼成 `luxShaFa` / `luxCanTing`，
> 但它们的**探针**读的是同一个 did。
> **勘查的人：** 他家老人多，别搞手动锁那套。
> **配置的人：** 那 `manualLock` 关掉，`shouDong*` 变量和那七条规则都不生成，
> 开灯链上就少一道**闸门** —— 看板会自己发现少了这道，不用改配置。

## Flagged ambiguities

- 「房间」在住户口中 = 物理房间，在看板里 = **区**。1302 的「主卧」和「卧室书桌」
  是两个区但住户会说成两个房间，而「进门」这个区住户叫「客厅」。
  已解决：配置里区名用住户的说法，规则名用区名，两边靠**覆盖层**对齐。
- 「模式」曾同时指模式变量（`guanYing`）和看板上的一组卡片。
  已解决：变量叫**模式开关**（一种**角色**），看板上那组叫分组。
